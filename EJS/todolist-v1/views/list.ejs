<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo list</title>
  </head>
  <body>
    <% if (kindOfDay === 'Saturday' || kindOfDay === 'Sunday'){ %>

    <h1 style="color: blueviolet"><%= kindOfDay %> list!</h1>

    <% } else{ %>

    <h1 style="color: limegreen"><%= kindOfDay %> list!</h1>

    <% } %>
  </body>
</html>
